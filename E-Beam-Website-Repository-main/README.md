# E-Beam-Website-Repository
Website for the lithography team.

Open up a local host via running the following in terminal:
python3 -m http.server 8000

Then, run input the following into your internet browser of choice, such as chrome, to display website:
http://localhost:8000/

All code runs in browser to avoid interactions with a nonexistant centralized server
