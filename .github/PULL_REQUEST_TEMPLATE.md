## Summary

- **What** does this PR add, change, or remove?
- **Paths** touched (e.g. `nanotech_website/app/...`, `server/routes/...`)

## Testing

- [ ] Tested locally with `./scripts/start_server.sh`
- [ ] `pnpm build` succeeds (if frontend changed)
- [ ] Backend starts without errors (if server changed)

## Deploy

- [ ] Needs `./scripts/deploy.sh` on production after merge
- [ ] Needs new env vars (list them; update `deploy/*.env.example`)
- [ ] Needs Pi agent redeploy (list which Pis)

## Screenshots

<!-- If UI changed, add screenshots -->
